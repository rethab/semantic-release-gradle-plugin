plugins {
  id 'maven-publish'
  id 'io.github.rethab.semantic-release' version '0.0.1'
}

group = 'io.github.rethab.semantic-test'
version = '1.2.3'

semanticRelease {
  releaseBranches = ['feat/configurable-release-branches'] // just for testing
}

publishing {
  repositories {
    mavenLocal()
  }
  publications {
    mavenJava(MavenPublication) {
    }
  }
}
